apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "jaeger.fullname" . }}-config
  labels:
    {{- include "jaeger.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- include "jaeger.config" . | nindent 4 }}
